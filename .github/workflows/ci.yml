name: CI

on: [push]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18
    - name: Install
      run: yarn install
    - name: Run Lint
      run: yarn lint:ci
    - name: Run Tests
      run: yarn test:ci
